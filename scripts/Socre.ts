import { _decorator, Component, v3,Label } from 'cc';
const { ccclass, property } = _decorator;

@ccclass('Socre')
export class Socre extends Component {
    
    start() {
        // this.node.setPosition(v3(this.node.getPosition().x,window.innerHeight/2,0))
    }

    update(deltaTime: number) {
        
    }

    public UpadateScore(){

    }
}

