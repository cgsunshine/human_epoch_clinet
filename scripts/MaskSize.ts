import { _decorator, Component, UITransform ,Size} from 'cc';
const { ccclass, property } = _decorator;

@ccclass('MaskSIze')
export class MaskSIze extends Component {
    start() {
        // this.node.getComponent(UITransform).setContentSize(new Size(window.innerHeight,window.innerWidth))
  
    }

    update(deltaTime: number) {
        
    }
}


